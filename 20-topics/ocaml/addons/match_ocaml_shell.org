:PROPERTIES:
:ID:       1b794026-5684-40c3-a605-285c5829b2c4
:END:
#+title: OCaml 和 Shell 的模式匹配
* OCaml 模式匹配与 Shell ~case ... in ...~ 对比

我们已经接触过 Shell 的：

#+begin_src bash
  case "$value" in
      pattern1)
  	commands
  	;;
      pattern2)
  	commands
  	;;
  esac
#+end_src

现在正在学习 OCaml 的:

#+begin_src ocaml
  match value with
  | pattern1 -> expression1
  | pattern2 -> expression2
#+end_src

会感觉到两者很相似。它们都采用一种“按顺序尝试候选模式，并执行第一个成功分支”的控制结构。

然而，OCaml 与 Shell 对 “模式” 的理解有很大区别。 Shell 的 ~case~ 主要处理文本分类； OCaml 的模式匹配可以直接观察数据结构、提取内部字段，并在编译阶段接受类型检查与完整性检查。

理解这一差异，是进入 OCaml 代数数据类型、递归数据结构和函数式编程思想的重要一步。

-----
* 最接近的例子

假设我们需要根据命令行参数执行不同操作。

Shell 可以这样写：
#+begin_src bash
  case "$1" in
      start)
  	echo "starting"
  	;;
      stop)
  	echo "stopping"
  	;;
      restart)
  	echo "restarting"
  	;;
      *)
  	echo "unknown command"
  	;;
  esac    
#+end_src

OCaml 中可以写成：

#+begin_src ocaml
  let describe_command command =
    match command with
      | "start" -> "starting"
      | "stop" -> "stopping"
      | "restart" -> "restarting"
      | _ -> "unknown command"
#+end_src

两套代码都遵循同一个基本流程：

1. 取得一个特定的值
2. 按照从上到下的顺序检查分支
3. 执行对应代码
4. 后续分支不再继续尝试

Bash 官方手册明确说明， ~case~ 中第一个匹配成功的模式决定将要执行的命令列表；通常 ~*~ 放在最后，作为匹配任意字符串的默认分支。 OCaml 的 ~match~ 也按照分支顺序进行选择。

可以先建立一组对应关系：

| Shell           | OCaml              | 作用                 |
|-----------------+--------------------+----------------------|
| ~case value in~ | ~match value with~ | 指定待匹配的值       |
| ~start)~        | ~\vert "start" ->~ | 声明一个候选分支     |
| ~*~             | ~_~                | 兜底模式             |
| ~command-list~  | ~expression~       | 匹配成功后执行的内容 |
| ~esac~          | 无需结束关键字     | 结束整个结构         |

不过这张表只描述了最基础的相似性。接下来需要逐步拆开两者的差异。

----
* Shell 的 ~case~: 面向字符串的 glob 模式分类


